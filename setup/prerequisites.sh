#
# Following packages need to be installed, if all of them are already installed on your system there is no need to
# run this script.
# Note that you need sudo rights to run this script.
#

apt-get apt-get install -yq \
      automake \
      bc \
      curl \
      g++ \
      git \
      libc-dev \
      libreadline-dev \
      libtool \
      make \
      ncurses-dev \
      nvi \
      pkg-config \
      python3 \
      python3-dev \
      python3-setuptools \
      python3-pip \
      unzip \
      wavpack \
      wget \
      zip \
      zlib1g-dev \
      sox \
      swig
